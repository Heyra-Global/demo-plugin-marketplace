# Connectors

None. Both skills in this plugin work on the conversation itself and need
no connector, no file access and no external service.

`ama` uses Claude's interactive question widget where the surface offers
it (Claude.ai chat) and falls back to plain questions elsewhere.
